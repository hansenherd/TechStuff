Two methods to run an awk script...
  - one with the awk program in a file, the other with the program inline
  - to launch either script, pass the data filename


additions, error check to make sure a $1 is passed and that it's a real file

